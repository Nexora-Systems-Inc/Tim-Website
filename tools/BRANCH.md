# Permanent developer tooling branch — never merge into main.

This branch (`tools/playwright-visual-reports`) hosts internal Playwright visual reporting tooling only.

- Sync **from** `main` when production evolves.
- Do **not** merge this branch back into `main`.

See `tools/visual-reports/README.md` for setup and usage.
